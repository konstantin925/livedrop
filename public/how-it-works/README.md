Replace these starter files with your final custom artwork while keeping filenames:

- `find-deals.png`
- `drop-activate.png`
- `save-money.png`
- Optional fallback: `fallback.svg`

These files are used by `HOW_IT_WORKS_CARDS` in:

`src/App.tsx`

If you want different filenames or formats, update the `image` paths in that mapping.
